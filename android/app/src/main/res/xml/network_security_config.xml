<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Allow HTTP traffic for development and testing -->
        <!-- Replace with your actual API domain in production -->
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">test.mindwell.win</domain>
        <!-- Add your production API domain here -->
        <!-- <domain includeSubdomains="true">your-api-domain.com</domain> -->
    </domain-config>
    
    <!-- For production, consider removing cleartextTrafficPermitted="true" 
         and only allow HTTPS connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust the system certificate authorities -->
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
